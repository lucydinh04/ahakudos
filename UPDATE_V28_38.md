# UPDATE V28.38 — Home empty-state logic & layout balance

- Empty onboarding card now appears only when the current employee has **not sent any KUDOS yet** (`sentCount === 0`).
- This fixes the contradictory state where “Gửi KUDOS đầu tiên” and “Chúc mừng bạn đã hoàn thành KUDOS đầu tiên” could appear at the same time.
- Reduced heading, icon, badge, body-copy and CTA sizes so the 3-step onboarding block balances better with the right rail cards.
- Updated empty-state subcopy to “Chưa gửi KUDOS nào?...”.
